1.二次开发,首先要对每个文件夹,每个文件到底是做什么的有印象
2.Breakcrumb:面包屑导航
3.写组件->配置路由(meta参数等)->
4.数据展示
  #api/trademark.js
    导入request.js
    导出req请求接口函数
  #views/product/trademark/index.js
    使用trycatch处理报错
    使用elementUI 的message 提示错误
  #vue.config.js
    配置proxy代理服务器
  #request.js
    改拦截器代码,改为不是200并且不是20000
    或者将mock/user.js的20000改为200
    当然也可以全部替换为200
  #requestMock.js
    baseURL改mock-api
    同user.js mock/mock-server.js(这个是webpack处理的)
5.详细代码:
  #utils/request.js
  {
    reqponse=>{
      const res=response.data
      if (res.code !== 200) {
        ......
    }
  }
  #utils/requestMock.js
  {
    const service=axios.create({
      baseURL:'/mock-api',
      timeout:5000
    })
  }
  #api/user.js
  {
    import request from '@/utils/requestMock'
  }
  #mock/mock-server.js
  {
    const responseFake=(url,type,respond)=>{
      return {
        url:new RegExp(`/mock-api${url}`),
        ...
      }
    }
  }
6.东说的所有20000都要改200
7.对话框,模态框,dialog和modal
  visible觉得dialog显示\隐藏
  .sync是同步的意思??
  :before-close关闭之前,触发函数
  组件的ref是给组件获取组件实例对象的
  label-width左边文字的宽度
  上传图片:
    action上传图片的地址
    size一般都是以bite单位
  2021年3月20日10:50:19 摸鱼 讲表单校验规则
  validater的callback必须调用,否则后面就会卡住
  trigger:"blur" 再失去焦点时进行触发
8.放弃了,老师的也是一样的问题,直接进行测试会出现问题,那个写好的好像是处理了一下mock之后才成功的?
9.原来是token的问题
  api/user.js 不用requestMock
  登录改为真实的信息
  在权限的接口
  getInfo在拦截器中整
  request.js中的X-Token改为token
  username:admin  password:111111
  这时候返回的code又是20000了
  所以request还是得进行并且
  vue.config.js 的端口号去掉
  http协议端口号默认80,https是443
  改完之后就不需要mock了
10.一开始上来就报错cannot read property clearValidate
  是因为dialog/form表单一开始是没有的,但是点击了之后就会立即触发一次清除校验结果
  改界面是异步的,代码调用方法是同步的,代码调用的时候还没有界面呢
11.🍔🍔🍔下一次是属性管理
12.哈希算法
  
  



